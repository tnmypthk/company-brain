# Company Brain — pinned for reproducible deploys (Streamlit Cloud + local).
# Versions are pinned exactly so the Cloud build resolves the same tree we
# tested locally. If you deploy on Streamlit Cloud, set the Python version to
# 3.11 in the app's Advanced settings — these pins were resolved on 3.9/3.11.
#
# NOTE: crewai was removed. It was listed but never imported (the agents are
# plain Claude API calls), and its 0.5.0 pin caps at Python 3.9 — it would
# break the Streamlit Cloud build. Re-add only if you actually use it.

# Core AI
anthropic==0.109.1
sentence-transformers==5.1.2

# Vector DB
chromadb==1.5.9
# Modern SQLite for hosts whose system sqlite3 is too old for ChromaDB
# (e.g. Streamlit Cloud). Linux-only marker so local macOS installs skip it.
# Intentionally unpinned — the right build depends on the host's glibc.
pysqlite3-binary; platform_system == "Linux"

# Document parsing
PyPDF2==3.0.1
python-docx==1.2.0

# Google Drive + Gmail
google-api-python-client==2.197.0
google-auth==2.50.0
google-auth-httplib2==0.3.1
google-auth-oauthlib==1.3.1

# Slack
slack_sdk==3.42.0

# Dashboard
streamlit==1.50.0

# Utilities
python-dotenv==1.2.1
tqdm==4.68.2
PyYAML==6.0.3
